"use client";

export default function UploadBox() {
  return (
    <button
      onClick={() => alert("Button works!")}
      style={{
        padding: "20px",
        fontSize: "20px",
        margin: "40px",
      }}
    >
      Click Me
    </button>
  );
}
